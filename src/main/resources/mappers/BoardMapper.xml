<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.study.api.Mapper.BoardMapper">
    <select id="selectBoard" resultType="com.example.study.api.VO.BoardVO">
        select * from study_board where board_id = #{board_id}
    </select>

</mapper>
